
export interface Status {
  id: string;
  name: string;
  color: string;
  description: string;
  is_active: boolean;
}

export interface CustomerAreaField {
  id: string;
  field_name: string;
  visible: boolean;
}
